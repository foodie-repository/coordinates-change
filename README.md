# 지번주소 좌표 변환 가이드

## 개요
엑셀 파일에 있는 지번주소를 카카오 Local API를 사용하여 위도/경도 좌표로 변환하는 스크립트입니다.

## 사전 준비

### 1. 카카오 REST API 키 발급
1. [카카오 개발자 사이트](https://developers.kakao.com/) 접속
2. 로그인 후 "내 애플리케이션" 메뉴로 이동
3. "애플리케이션 추가하기" 클릭
4. 앱 이름, 사업자명 입력 후 저장
5. 생성된 앱의 "앱 키" 탭에서 **REST API 키** 복사

### 2. 필요한 라이브러리 설치
```bash
cd /Users/foodie/myproject
source venv/bin/activate
pip install openpyxl requests pandas
```

## 사용 방법

### 실행
```bash
cd /Users/foodie/myproject
source venv/bin/activate
python add_coordinates.py
```

실행하면 카카오 REST API 키를 입력하라는 메시지가 나옵니다.
발급받은 API 키를 입력하면 자동으로 처리가 시작됩니다.

### 입력 파일
- **위치**: `/Users/foodie/Downloads/서울.xlsx`
- **필수 컬럼**:
  - `지번주소`: 변환할 주소 (예: "서울특별시 송파구 풍납동 260-4")
  - `위도`: 변환된 위도가 저장될 컬럼
  - `경도`: 변환된 경도가 저장될 컬럼

### 출력 파일
- **위치**: `/Users/foodie/Downloads/서울_좌표추가.xlsx`
- 원본 파일은 유지되고 새로운 파일이 생성됩니다

## 주요 기능

### 1. 자동 주소 검색
- 카카오 Local API를 통해 지번주소를 위도/경도로 변환
- 지번주소가 없으면 도로명주소로 대체

### 2. 중복 처리 방지
- 이미 좌표가 입력된 행은 건너뛰기
- 중단 후 재실행 시 처리되지 않은 주소만 처리

### 3. API 제한 고려
- 카카오 API 호출 제한(초당 10회)을 고려한 자동 대기 시간 설정
- 각 요청 사이에 0.1초 대기

### 4. 상세한 로그
- 처리 진행 상황 실시간 표시
- 성공/실패 건수 요약 제공

## 코드 구조

### 함수 설명

#### `get_coordinates(address, api_key)`
- **목적**: 단일 주소를 위도/경도로 변환
- **입력**:
  - `address`: 지번주소 문자열
  - `api_key`: 카카오 REST API 키
- **출력**: `(위도, 경도)` 튜플, 실패 시 `(None, None)`

#### `process_excel_file(input_file, output_file, api_key)`
- **목적**: 엑셀 파일 전체 처리
- **입력**:
  - `input_file`: 입력 엑셀 파일 경로
  - `output_file`: 출력 엑셀 파일 경로
  - `api_key`: 카카오 REST API 키
- **처리 과정**:
  1. 엑셀 파일 읽기
  2. 각 행의 지번주소 처리
  3. 위도/경도 업데이트
  4. 결과 파일 저장
  5. 처리 결과 요약 출력

## 에러 처리

### API 에러
- 네트워크 에러나 API 응답 오류 시 해당 주소는 건너뛰고 계속 진행
- 에러 발생 시 상세 메시지 출력

### 주소 검색 실패
- 주소를 찾을 수 없는 경우 경고 메시지 출력
- 해당 행의 위도/경도는 빈 값으로 유지

## 실행 예시

```
카카오 REST API 키를 입력하세요: your_api_key_here

🚀 주소 좌표 변환을 시작합니다...

📖 파일 읽는 중: /Users/foodie/Downloads/서울.xlsx
📊 총 568개의 주소를 처리합니다.
--------------------------------------------------
🔍 [1/568] 검색 중: 서울특별시 송파구 풍납동 260-4
✅ 성공: 위도=37.5321, 경도=127.1234
🔍 [2/568] 검색 중: 서울특별시 송파구 오금동 165
✅ 성공: 위도=37.5012, 경도=127.1345
...
--------------------------------------------------
💾 결과 저장 중: /Users/foodie/Downloads/서울_좌표추가.xlsx

==================================================
📊 처리 결과
==================================================
✅ 성공: 560개
❌ 실패: 8개
📁 저장 위치: /Users/foodie/Downloads/서울_좌표추가.xlsx
==================================================
```

## 주의사항

1. **API 키 보안**: API 키는 공개되지 않도록 주의하세요
2. **API 할당량**: 카카오 API는 무료 할당량이 있습니다 (일일 30만건)
3. **처리 시간**: 568개 주소 처리 시 약 1분 소요
4. **인터넷 연결**: API 호출을 위해 인터넷 연결 필요

## 문제 해결

### "API 키가 유효하지 않습니다" 에러
- 카카오 개발자 사이트에서 API 키를 다시 확인하세요
- REST API 키를 사용하는지 확인하세요 (JavaScript 키 X)

### "응답이 없습니다" 에러
- 인터넷 연결을 확인하세요
- 카카오 API 서버 상태를 확인하세요

### 특정 주소 검색 실패
- 주소가 정확한지 확인하세요
- 통폐합된 주소나 신규 주소는 검색되지 않을 수 있습니다
